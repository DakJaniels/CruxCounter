-- -----------------------------------------------------------------------------
-- Addon.lua
-- -----------------------------------------------------------------------------

--- @class (partial) CruxCounter
CruxCounter = {}

--- @type { name: string, version: string } General addon information
local M = {
    name    = "CruxCounter",
    version = "1.4.0",
}

CruxCounter.Addon = M
